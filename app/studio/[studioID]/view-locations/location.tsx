// eslint-disable-next-line import/extensions
//import Link from 'next/link';

export default function Location({ location }) {
  // Generate a location component based on the data inside the location props

  return (
    <div>

    </div>
  );
}
